name: 'incidentlens'
acli_version: '1.6.1'
agent_directory: 'app'
region: 'us-east1'
base_template: 'local_incidentlens'
generated_at: '2026-09-17T13:03:44.682578+00:00'
language: 'python'
create_params:
  # The ADK name of the root agent, as rendered into the agent source. Rename
  # the agent and you must change it in both places: telemetry reports this as
  # gen_ai.agent.name, and it is what agents-cli reports for the project.
  root_agent_name: 'incidentlens'
  deployment_target: 'none'
  session_type: 'in_memory'
  cicd_runner: 'skip'
  is_a2a: true
  agent_gateway: False
  agent_guidance_filename: 'AGENTS.md'
